import { ReactNode } from 'react'

export interface NBChunksProviderProps {
    children: ReactNode;
}

export interface NBMetadataProviderProps {
    children: ReactNode;
}

export interface NBTimestampProviderProps {
    children: ReactNode;
}